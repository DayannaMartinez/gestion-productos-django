[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate && gunicorn gestion_productos.wsgi --log-file -"
healthcheckPath = "/"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
